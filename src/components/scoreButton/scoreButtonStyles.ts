import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 48,
    height: 32,
    alignItems: "center",
    justifyContent: "center",
    borderWidth: 1,
    borderRadius:6
  },
  label: {
    fontSize: 20,
    fontWeight:"bold"
  }
})